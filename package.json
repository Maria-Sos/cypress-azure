{
  "name": "cypres_azure",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:open": "npx cypress open",
    "test:run": "npx cypress run --browser chrome --spec cypress/e2e/1-getting-started",
    "delete:reports": "rm results/* || true",
    "test": "npm run test:run --reporter junit \\\n  --reporter-options \"mochaFile=results/my-test-output.xml,toConsole=true\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.12.0"
  }
}
